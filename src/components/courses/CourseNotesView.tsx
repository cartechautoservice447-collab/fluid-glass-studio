import { AnimatePresence, motion } from "motion/react";
import { CalendarDays, Clock3, PanelLeft, ShieldCheck, X } from "lucide-react";
import { NoteEditor } from "@/components/notes/NoteEditor";
import { NoteList } from "@/components/notes/NoteList";
import { Sidebar } from "@/components/notes/Sidebar";
import type { Course } from "@/hooks/useCourses";
import { useNotes } from "@/hooks/useNotes";
import { useState } from "react";

type Props={course:Course;onBack:()=>void;onLogout:()=>void;userId:string;email:string|null;studyHubMode?:boolean};
const DISTRACTION_KEY=(userId:string)=>`liquid-glass-distraction-mode:${userId}`;
export function CourseNotesView({course,onBack,onLogout,userId,email,studyHubMode=false}:Props){const notes=useNotes(course.id,userId,studyHubMode?"study-hub":"editor");const{filter}=notes;const[minimized,setMinimized]=useState({sidebar:false,notes:false,editor:false});const[distractionOpen,setDistractionOpen]=useState(false);const[confirmOpen,setConfirmOpen]=useState(false);const[durationHours,setDurationHours]=useState(1);const[date,setDate]=useState(new Date().toISOString().slice(0,10));const hasMinimizedPanel=minimized.sidebar||minimized.notes||minimized.editor;const showAllNotesInEditor=minimized.sidebar&&minimized.notes;const heading=filter.kind==="all"?"All Notes":filter.kind==="favorites"?"Favorites":(notes.collections.find(c=>c.id===filter.id)?.name??"Collection");
const startDistraction=()=>{const hours=Math.min(24,Math.max(1,Number(durationHours)||1));const session={active:true,courseId:course.id,courseName:course.name,date,durationHours:hours,endsAt:Date.now()+hours*60*60*1000};const value=JSON.stringify(session);localStorage.setItem(DISTRACTION_KEY(userId),value);setConfirmOpen(false);setDistractionOpen(false);window.dispatchEvent(new StorageEvent("storage",{key:DISTRACTION_KEY(userId),newValue:value}));};
return <div className="relative flex h-full min-h-0 w-full flex-col gap-[10px]"><div className="relative flex min-h-0 w-full flex-1 flex-row items-stretch gap-[16px] overflow-hidden"><AnimatePresence initial={false}>{!minimized.sidebar&&<motion.div key="sidebar" layout initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:.98}} transition={{duration:.2,ease:"easeOut"}} className={hasMinimizedPanel?"h-full min-w-[220px] flex-1 basis-0":"h-full w-[286px] min-w-[220px] shrink basis-[286px]"}><Sidebar minimized={false} onMinimize={()=>setMinimized(c=>({...c,sidebar:!c.sidebar}))} email={email} onBack={onBack} onLogout={onLogout} collections={notes.collections} counts={notes.counts} filter={notes.filter} setFilter={notes.setFilter} query={notes.query} setQuery={notes.setQuery} onCreateNote={notes.createNote} onAddCollection={notes.addCollection} onRenameCollection={notes.renameCollection} onDeleteCollection={notes.deleteCollection}/></motion.div>}{!minimized.notes&&<motion.div key="notes" layout initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:.98}} transition={{duration:.2,ease:"easeOut"}} className={hasMinimizedPanel?"h-full min-w-[240px] flex-1 basis-0":"h-full w-[318px] min-w-[240px] shrink basis-[318px]"}><NoteList minimized={false} onMinimize={()=>setMinimized(c=>({...c,notes:!c.notes}))} notes={notes.visibleNotes} selectedId={notes.selectedId} onSelect={notes.setSelectedId} onToggleFavorite={notes.toggleFavorite} heading={heading}/></motion.div>}{!minimized.editor&&<motion.div key="editor" layout initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:.98}} transition={{duration:.2,ease:"easeOut"}} className={hasMinimizedPanel?"h-full min-w-[280px] flex-[1.35] basis-0":"h-full min-w-[280px] flex-1"}><NoteEditor minimized={false} onMinimize={()=>setMinimized(c=>({...c,editor:!c.editor}))} note={notes.selected} notes={notes.notes} onSelectNote={notes.setSelectedId} showAllNotes={showAllNotesInEditor} collections={notes.collections} onUpdate={notes.updateNote} onDelete={notes.deleteNote} onToggleFavorite={notes.toggleFavorite}/></motion.div>}</AnimatePresence></div>{hasMinimizedPanel&&<div className="absolute bottom-4 right-4 z-10 flex items-center gap-2 rounded-full border border-white/25 bg-white/10 p-1.5 text-foreground backdrop-blur-xl">{minimized.sidebar&&<button type="button" onClick={()=>setMinimized(c=>({...c,sidebar:false}))} className="flex size-8 items-center justify-center rounded-full hover:bg-white/20" aria-label="Restore navigation panel"><PanelLeft className="size-4"/></button>}{minimized.notes&&<button type="button" onClick={()=>setMinimized(c=>({...c,notes:false}))} className="flex size-8 items-center justify-center rounded-full hover:bg-white/20" aria-label="Restore notes panel"><PanelLeft className="size-4"/></button>}{minimized.editor&&<button type="button" onClick={()=>setMinimized(c=>({...c,editor:false}))} className="flex size-8 items-center justify-center rounded-full hover:bg-white/20" aria-label="Restore editor panel"><PanelLeft className="size-4"/></button>}</div>}<button type="button" onClick={()=>setDistractionOpen(true)} className="absolute bottom-5 left-5 z-20 flex items-center gap-2 rounded-full border border-white/20 bg-black/25 px-4 py-2 text-xs font-semibold text-white shadow-lg backdrop-blur-xl hover:bg-black/40" aria-label="Open Distraction Mode"><ShieldCheck className="size-4"/>Distraction Mode</button>
{distractionOpen&&<div className="fixed inset-0 z-[150] flex items-center justify-center bg-black/35 p-5 backdrop-blur-md" role="dialog" aria-modal="true"><div className="w-full max-w-md rounded-[30px] border border-white/20 bg-black/40 p-6 shadow-2xl backdrop-blur-2xl"><div className="flex items-center justify-between"><div><p className="text-lg font-semibold">Distraction Mode</p><p className="mt-1 text-xs text-muted-foreground">Study inside this course folder.</p></div><button type="button" onClick={()=>setDistractionOpen(false)} className="rounded-full p-2 text-muted-foreground hover:bg-white/10" aria-label="Close"><X className="size-4"/></button></div><div className="mt-5 rounded-2xl border border-white/10 bg-white/[.05] p-4"><div className="flex items-center gap-2 text-xs text-muted-foreground"><ShieldCheck className="size-4"/>Selected course</div><p className="mt-1 text-base font-semibold">{course.name}</p></div><div className="mt-4 grid grid-cols-2 gap-3"><label className="block"><span className="mb-2 block text-xs font-medium text-muted-foreground"><Clock3 className="mr-1 inline size-3.5"/>Study hours</span><input type="number" min={1} max={24} value={durationHours} onChange={e=>setDurationHours(Math.min(24,Math.max(1,Number(e.target.value)||1)))} className="w-full rounded-2xl border border-white/10 bg-white/[.05] px-4 py-3 text-sm outline-none"/></label><label className="block"><span className="mb-2 block text-xs font-medium text-muted-foreground"><CalendarDays className="mr-1 inline size-3.5"/>Date</span><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="w-full rounded-2xl border border-white/10 bg-white/[.05] px-4 py-3 text-sm outline-none"/></label></div><button type="button" onClick={()=>setConfirmOpen(true)} className="mt-5 flex w-full items-center justify-center gap-2 rounded-2xl bg-foreground px-5 py-3 text-sm font-semibold text-background"><ShieldCheck className="size-4"/>Continue</button></div></div>}
{confirmOpen&&<div className="fixed inset-0 z-[180] flex items-center justify-center bg-black/55 p-5 backdrop-blur-md" role="alertdialog" aria-modal="true"><div className="w-full max-w-sm rounded-[28px] border border-white/20 bg-black/45 p-6 text-center shadow-2xl backdrop-blur-2xl"><div className="mx-auto flex size-14 items-center justify-center rounded-2xl border border-white/15 bg-white/[.07]"><ShieldCheck className="size-6"/></div><h3 className="mt-5 text-xl font-semibold">Start Distraction Mode?</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{course.name} will stay open for {durationHours} hour{durationHours===1?"":"s"}. Back and exit controls are unavailable until the timer ends.</p><div className="mt-5 grid grid-cols-2 gap-2"><button type="button" onClick={()=>setConfirmOpen(false)} className="rounded-2xl border border-white/10 bg-white/[.05] px-4 py-3 text-sm font-medium">Cancel</button><button type="button" onClick={startDistraction} className="rounded-2xl bg-foreground px-4 py-3 text-sm font-semibold text-background">Confirm & Lock</button></div></div></div>}</div>;
}
