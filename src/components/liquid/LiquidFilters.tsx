/**
 * Legacy placeholder retained for existing imports.
 *
 * Real Liquid Glass refraction is now rendered by the shared WebGL engine
 * in LiquidGlassWebGL.tsx, so the old SVG filter must not remain active.
 */
export function LiquidFilters() {
  return null;
}
